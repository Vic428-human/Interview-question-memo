

```
export const extractDisplayUnitFromUUID = async (uuid: string) => {
  const [
    sourceContextId,        // 此 UUID 所屬的情境來源
    groupingIdentifier,     // 分類或分群用的唯一識別
    targetDisplayId,        // 前端要呈現的顯示單元 ID
    behaviorPresetType,     // 控制顯示/行為的預設類型
    logicCodeSeed,          // 提供邏輯分歧依據的 code
    hierarchyLevelKey,      // 決定層級關係的關鍵索引
  ] = parseProductUUid(uuid);

  const logicBranchKey = codeDict[logicCodeSeed];

  if (logicBranchKey === 'SKIP_PROCESSING') return undefined;

  const logicToViewModeMap = {
    STATIC_VIEW: 'static_mode',
    INTERACTIVE_VIEW: 'interactive_mode',
    CUSTOM_VIEW: 'custom_mode',
  } as const;

  // ➤ 可依照 logicToViewModeMap[logicBranchKey] 做不同處理
};
```
